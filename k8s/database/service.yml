apiVersion: v1
kind: Service
metadata:
  name: postgres-service
  namespace: food-delivery
spec:
  selector:
    app: postgres-database
  ports:
    - name: postgres
      port: 5432
      targetPort: 5432
  clusterIP: None
  type: ClusterIP
